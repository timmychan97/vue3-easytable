:::tip
1. Configure the row expand feature via the `expandOption` property.<br>
2. Set `type=expand` in `columns` to define the expand column.
3. Set the `rowKeyFieldName` property to the corresponding row data column name.<br>
4. The `render` function allows custom expand content, using JSX syntax here.<br>

:::
