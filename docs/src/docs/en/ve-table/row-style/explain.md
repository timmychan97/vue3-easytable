:::tip
1. Set row styles via the `rowStyleOption` property.<br>
2. Enable striped rows with `stripe=true`.<br>
3. Enable row hover highlight with `hoverHighlight=true`. Enabled by default.<br>
4. Enable row click highlight with `clickHighlight=true`. Enabled by default.

:::
