::: tip
1. Angi radstiler via egenskapen `rowStyleOption`.<br>
2. Aktiver stripete rader med `stripe=true`.<br>
3. Aktiver utheving ved hover med `hoverHighlight=true`. Aktivert som standard.<br>
4. Aktiver utheving ved klikk med `clickHighlight=true`. Aktivert som standard.

:::
